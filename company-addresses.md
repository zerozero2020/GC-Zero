# Company Addresses

Master list of AVDG client sites (and any other recurring locations). The calendar skills read this file to auto-fill the `location` field on events tagged with a known company.

Keep one company per row. The **Company** column should match exactly what shows up in event titles between the parentheses (`AVDG (Hines)` → match on `Hines`). Casing doesn't matter for the lookup, but stay consistent for readability.

| Company | Address | Notes |
|---|---|---|
| Hines |  |  |
| PAN |  |  |
| Hunterdon Health |  |  |
| Citrin Cooperman |  |  |
| Asana |  |  |
| Instacart |  |  |

Add rows as new sites come up. Empty addresses are fine — the skills will just skip the location field for those until you fill them in.
